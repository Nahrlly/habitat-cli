Open the human-facing Kepler docs at https://planet.turingguild.com/docs.
Also inspect the agent-readable OpenAPI schema at https://planet.turingguild.com/openapi.json.
Dont let `index.ts` get too big, make sure to break things up as best you can.
Do not delete parts of the code that you will rewrite the exact same, instead of writing index.ts files from scratch only edit what is needed.
